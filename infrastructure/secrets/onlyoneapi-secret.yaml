apiVersion: v1
kind: Secret
metadata:
  name: onlyoneapi-credentials
  namespace: nexia-voice-dev
type: Opaque
data:
  # Base64 encoded API key (will be injected via DigitalOcean secrets)
  api-key: # TO_BE_SET_VIA_DOCTL_OR_KUBECTL
stringData:
  # Example for local development only (will be overridden in production)
  # api-key: "sk-your-api-key-here"
---
apiVersion: v1
kind: Secret
metadata:
  name: onlyoneapi-credentials
  namespace: nexia-voice-prod
type: Opaque
data:
  # Production API key (must be set via DigitalOcean secrets management)
  api-key: # TO_BE_SET_VIA_DOCTL_SECURE_INJECTION